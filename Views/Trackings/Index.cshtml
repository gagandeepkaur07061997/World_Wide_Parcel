@model IEnumerable<World_Wide_Parcel.Models.Tracking>

@{
    ViewData["Title"] = "Index";
}
<!--code to change the test' position from left to right-->
<div class="text-center">

    <header>
        <!--z-index code to display image on the Tracking page behind the text-->
        <style>
            img {
                position: absolute;
                left: 0px;
                top: 0px;
                z-index: -1;
                height: 100%;
                width: 100%;
                object-fit: cover;
            }
        </style>
    </header>

    <img src="~/images/tracking.jpg" height=" 800" weight="1500" />
    <!--code to increse the size of heading and to change its color and bold it-->
    <h1 class="display-4"><b><p style="color:black;">Track your parcel</b></h1></p>
</div>
<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Expected_date_of_delivery)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Parcels)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Expected_date_of_delivery)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Parcels.Content_type)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
